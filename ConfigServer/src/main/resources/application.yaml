server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/meryemciftci/microservice-config.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "meryemciftci"
          password: ${GIT_KEY}
